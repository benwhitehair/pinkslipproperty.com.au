---
import Layout from '../layouts/layout.astro';
import { siteConfig } from '../lib/site-config';
---

<Layout title={`Message Sent | ${siteConfig.title}`}>
	<div class="prose m-auto">
		<h1 class="font-display uppercase">Message sent</h1>
		<p>Looks like the website has sprung a leak!</p>
		<p>Thank you for contacting Pinkslip Property.</p>
		<p>We will get back to you as soon as possible.</p>
		<p>
			Please call <a href={`tel:${siteConfig.phone}`}>
				{siteConfig.phone}
			</a> if you matter is urgent.
		</p>
		<p>
			<a href="/"> Click here to return to the homepage. </a>
		</p>
	</div>
</Layout>
